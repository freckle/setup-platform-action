{
  "name": "setup-platform-action",
  "version": "0.0.0",
  "description": "This file only exists to support installing build tools",
  "scripts": {
    "readme": "npx action-docs -u && prettier --write README.md"
  },
  "devDependencies": {
    "action-docs": "^2.5.1",
    "prettier": "^3.7.2"
  }
}
